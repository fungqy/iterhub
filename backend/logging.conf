[loggers]
keys=root,uvicorn,apscheduler

[handlers]
keys=consoleHandler,fileHandler

[formatters]
keys=standard

[logger_root]
level=INFO
handlers=consoleHandler,fileHandler

[logger_uvicorn]
level=INFO
handlers=consoleHandler,fileHandler
qualname=uvicorn
propagate=0

[logger_apscheduler]
level=INFO
handlers=consoleHandler,fileHandler
qualname=apscheduler
propagate=0

[handler_consoleHandler]
class=StreamHandler
level=INFO
formatter=standard
args=(sys.stdout,)

[handler_fileHandler]
class=handlers.RotatingFileHandler
level=INFO
formatter=standard
# 显式声明 UTF-8:不声明时编码跟随启动环境(locale/PYTHONUTF8),同一文件会混入两种编码
args=('logs/app.log', 'a', 10485760, 5, 'utf-8')

[formatter_standard]
format=%(asctime)s - %(name)s - %(levelname)s - %(message)s
datefmt=%Y-%m-%d %H:%M:%S
